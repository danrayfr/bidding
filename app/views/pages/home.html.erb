<% provide(:title, "")%>
<div class="flex items-center justify-between pb-6 border-b">
  <h1 class="font-bold text-5xl text-gray-900">Bidding</h1>
  <%  if user_signed_in?%>
    <p data-controller="hello" data-action="click->hello#toggle" class="text-gray-700 text-2xl">
      Hello, <%= current_user.full_name %>
    </p>
  <% end %>
</div>
<div class="flex flex-wrap -mx-1 lg:-mx-4">

</div>